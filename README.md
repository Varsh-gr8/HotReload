# HotReload
A lightweight CLI tool written in Go that watches a project folder for code changes, automatically rebuilds the project, and restarts the server. Designed to improve developer productivity by eliminating manual rebuilds and restarts. Supports nested project structures, robust process management, and real-time server logs.
